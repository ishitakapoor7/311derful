# Copy to .env.local to change behaviour.

# Mock backend is ON by default so the app runs with no backend at all.
# Set to false once /api/ask, /api/config, /api/history and /api/explore answer.
VITE_USE_MOCK=true
